# Dev-only tooling. NEVER installed on the Raspberry Pi runtime — the product
# ships from requirements.txt alone. Install locally with:
#   pip install -r requirements-dev.txt
ruff>=0.6
hypothesis>=6.100
# starlette's TestClient (tests/test_routes.py) refuses to import without it —
# the HTTP client it drives the app through. Named httpx2 since starlette 1.3;
# on an older starlette install plain `httpx` instead, the error message says which.
httpx2>=0.28
pytest-cov>=5.0
pytest-xdist>=3.6
pip-audit>=2.7
pre-commit>=3.7
